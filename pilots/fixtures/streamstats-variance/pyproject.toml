[project]
name = "streamstats-variance"
version = "0.1.0"
description = "Streaming variance estimators, and whether the shifted-data one is stable."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/streamstats"]
